const Properties = () => {
  return (
    <div className="space-y-4 pt-4">
      <header className="space-y-2">
        <h1 className="text-lg font-semibold">GSAP Properties</h1>
        <p className="text-sm text-gray-600">Common animatable properties like x, y, rotation, scale, opacity. GSAP normalizes units and provides transforms with high performance.</p>
      </header>
      <ul className="list-disc pl-6">
        <li>Properties</li>
      </ul>
    </div>
  );
};

export default Properties;
